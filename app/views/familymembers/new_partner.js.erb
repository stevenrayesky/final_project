$(".options_modal").append($("<%= j render partial: 'trees/new_partner', object: @partner, locals: {tree: @tree} %>"));
		var position = $('#partner_item').position();
		$('.new_familymember').css( {top: position.top, left: position.left + 100});
