$(".options_modal").append($("<%= j render partial: 'trees/new_parent', object: @child, locals: {tree: @tree} %>"));
		var position = $('#parent_item').position();
		$('.new_familymember').css( {top: position.top, left: position.left + 120});
