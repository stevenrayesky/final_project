$(".options_modal").append($("<%= j render partial: 'trees/new_child', object: @parent, locals: {tree: @tree} %>"));
		var position = $('#child_item').position();
		$('.new_familymember').css( {top: position.top, left: position.left + 120});
